<Window x:Class="MFPControlCenter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:MFPControlCenter.Views"
        mc:Ignorable="d"
        Title="MFP Control Center - HP LaserJet M1536dnf"
        Height="650" Width="1000"
        MinHeight="550" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" Grid.Row="0">
            <DockPanel>
                <materialDesign:PackIcon Kind="Printer" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <StackPanel>
                    <TextBlock Text="MFP Control Center" FontSize="20" FontWeight="Medium"/>
                    <TextBlock Text="HP LaserJet M1536dnf" FontSize="12" Opacity="0.8"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="Circle" Width="12" Height="12"
                                             Foreground="{Binding PrinterStatusColor}"
                                             VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding PrinterStatus}" VerticalAlignment="Center" FontSize="12"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Основное содержимое с табами -->
        <TabControl Grid.Row="1"
                    Style="{StaticResource MaterialDesignFilledTabControl}"
                    materialDesign:ColorZoneAssist.Mode="PrimaryLight">

            <!-- Вкладка Печать -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Printer" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Печать" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <views:PrintView/>
            </TabItem>

            <!-- Вкладка Сканер -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Scanner" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Сканер" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <views:ScanView/>
            </TabItem>

            <!-- Вкладка Копир -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentCopy" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Копир" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <views:CopyView/>
            </TabItem>
        </TabControl>

        <!-- Статус-бар -->
        <materialDesign:ColorZone Mode="PrimaryDark" Grid.Row="2" Padding="8,4">
            <DockPanel>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
                <ProgressBar x:Name="ProgressBar"
                             Value="{Binding Progress}"
                             Minimum="0" Maximum="100"
                             Width="200" Height="8"
                             HorizontalAlignment="Right"
                             Visibility="{Binding IsProgressVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Style="{StaticResource MaterialDesignLinearProgressBar}"/>
            </DockPanel>
        </materialDesign:ColorZone>
    </Grid>
</Window>
