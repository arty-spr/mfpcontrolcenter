<Window x:Class="MFPControlCenter.Views.PrinterSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Настройки изображения"
        Height="320" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Настройки изображения"
                   FontSize="18" FontWeight="Medium"
                   Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1">
            <!-- Яркость -->
            <StackPanel Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Яркость:" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Brightness}" FontWeight="Medium"/>
                </Grid>
                <Slider x:Name="BrightnessSlider"
                        Value="{Binding Brightness, Mode=TwoWay}"
                        Minimum="-50" Maximum="50"
                        TickFrequency="10"
                        IsSnapToTickEnabled="True"
                        Style="{StaticResource MaterialDesignDiscreteSlider}"/>
            </StackPanel>

            <!-- Контрастность -->
            <StackPanel Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Контрастность:" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Contrast}" FontWeight="Medium"/>
                </Grid>
                <Slider x:Name="ContrastSlider"
                        Value="{Binding Contrast, Mode=TwoWay}"
                        Minimum="-50" Maximum="50"
                        TickFrequency="10"
                        IsSnapToTickEnabled="True"
                        Style="{StaticResource MaterialDesignDiscreteSlider}"/>
            </StackPanel>

            <!-- Резкость -->
            <StackPanel Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Резкость:" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Sharpness}" FontWeight="Medium"/>
                </Grid>
                <Slider x:Name="SharpnessSlider"
                        Value="{Binding Sharpness, Mode=TwoWay}"
                        Minimum="0" Maximum="100"
                        TickFrequency="10"
                        IsSnapToTickEnabled="True"
                        Style="{StaticResource MaterialDesignDiscreteSlider}"/>
            </StackPanel>
        </StackPanel>

        <!-- Кнопки -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Сбросить"
                    Click="ResetButton_Click"
                    HorizontalAlignment="Left"
                    Style="{StaticResource MaterialDesignFlatButton}"/>

            <Button Grid.Column="1"
                    Content="Отмена"
                    IsCancel="True"
                    Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>

            <Button Grid.Column="2"
                    Content="Применить"
                    IsDefault="True"
                    Click="ApplyButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>
    </Grid>
</Window>
